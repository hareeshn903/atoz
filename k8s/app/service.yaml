apiVersion: v1
kind: Service

metadata:
  name: mini-account-management-service
  namespace: mini

spec:
  selector:
    app: mini-account-management

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: NodePort